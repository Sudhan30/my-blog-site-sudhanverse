{
    "name": "sudhanverse-bun-blog",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "bun run --watch server/index.ts",
        "start": "bun run server/index.ts",
        "build": "bun build server/index.ts --outdir dist --target bun"
    },
    "dependencies": {
        "marked": "^12.0.2",
        "gray-matter": "^4.0.3",
        "pg": "^8.11.3"
    },
    "devDependencies": {
        "@types/bun": "latest"
    }
}